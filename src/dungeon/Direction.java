package dungeon;

/**
 * This is the enum class to represent the Direction. It specifies the categorical attribute named
 * direction which indicates where the player can move.
 */
public enum Direction {
  NORTH, SOUTH, EAST, WEST
}
